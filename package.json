{
  "name": "portfolio",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "test-build": "vite build && vite preview",
    "deploy": "git add . && git commit -m $MESSAGE && git push -u origin main && git push heroku main"
  },
  "devDependencies": {
    "vite": "^2.6.14"
  },
  "dependencies": {
    "three": "^0.134.0"
  }
}
