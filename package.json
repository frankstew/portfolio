{
  "name": "portfolio",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build && ls -la",
    "serve": "vite preview",
    "test-build": "vite build && vite preview",
    "help": "node -e 'console.log(\"deploy: npm run deploy -- <single or double quote>commit message<single or double quote>\")'",
    "deploy": "git add . && git commit -m",
    "postdeploy": "git push -u origin main && git push heroku main"
  },
  "devDependencies": {
    "vite": "^2.6.14"
  },
  "dependencies": {
    "three": "^0.134.0"
  }
}
